#!/bin/bash
# MCP Server Runner for Markdown Exporter
# This script starts the MCP server to expose markdown conversion tools

# Import common script
source "$(dirname "${BASH_SOURCE[0]}")/script_runner.sh"

# Run the MCP server
run_python_script "mcp_server.py"
